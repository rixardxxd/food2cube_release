'use strict';

angular.module('food2cubeApp')
  .controller('OrderCompletionCtrl', ['$log','$scope', '$http','$location',
        function ($log,$scope, $http,$location)
        {

        }
    ]);

